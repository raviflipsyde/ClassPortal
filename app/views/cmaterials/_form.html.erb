<style>
  body {background-color:#2E2F30;}
  h1   {color:white;}
  p    {color:white;}
</style>
<%= form_for(@cmaterial) do |f| %>
  <% if @cmaterial.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cmaterial.errors.count, "error") %> prohibited this cmaterial from being saved:</h2>

      <ul>
      <% @cmaterial.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content, class:'form_lable' %><br>
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :course_id, class:'form_lable' %><br>
    <%= f.text_field :course_id ,:readonly => "readonly"%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
